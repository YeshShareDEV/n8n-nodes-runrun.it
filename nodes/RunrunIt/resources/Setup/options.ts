import type { INodeProperties } from 'n8n-workflow';

// Options for Setup resource (e.g., available contexts or LDAP lists). Populated later.
export const setupOptionsDescription: INodeProperties[] = [];
